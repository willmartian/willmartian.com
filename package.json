{
  "name": "willmartian.com",
  "private": true,
  "description": "Will Martin's personal website.",
  "type": "module",
  "scripts": {
    "build": "tailwindcss -i src/css/style.css -o _site/css/style.css && eleventy",
    "start": "tailwindcss -i src/css/style.css -o _site/css/style.css --watch & eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": {
    "name": "Will Martin",
    "url": "https://willmartian.com"
  },
  "license": "ISC",
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^1.2.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^3.1.1",
    "@tailwindcss/cli": "^4.0.15",
    "tailwindcss": "^4.0.15"
  }
}
