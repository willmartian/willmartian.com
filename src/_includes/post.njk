---
layout: page.njk
---
<section class="note card light"><b>Published:</b> <time datetime="{{ page.date }}">{{ page.date.toLocaleDateString() }}</time> by <a href="{{ '/about' | url}}">Will</a> 👽</section>

{{ content | safe }}
